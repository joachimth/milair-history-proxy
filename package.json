{
  "name": "milair-history-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for OpenSky historical flight data (OAuth2 + CORS) for MilAir Watch",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
