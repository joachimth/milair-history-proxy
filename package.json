{
  "name": "milair-history-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker that builds its own historical flight database (D1 + Cron) for MilAir Watch",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
